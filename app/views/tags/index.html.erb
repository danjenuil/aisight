<h1>Tags</h1>

<table>
  <thead>
    <tr>
      <th>Tag</th>
      <th>Total Images</th>
    </tr>
  </thead>
  <tbody>
    <% @tags.each do |tag| %>
      <tr>
        <td><%= tag.name %></td>
        <td><%= tag.images.count %></td>
      </tr>
    <% end %>
  </tbody>
</table>
